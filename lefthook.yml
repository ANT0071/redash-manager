pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{js,json,md,yml}'
      run: npx prettier --write {staged_files} && git add {staged_files}
    type-check:
      run: npm run type-check
